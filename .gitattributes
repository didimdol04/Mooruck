# 텍스트 파일은 기본적으로 커밋 시 LF로 정규화
* text=auto eol=lf

# 셸 스크립트는 항상 LF (Mac/Linux에서 실행 가능해야 함)
gradlew text eol=lf
*.sh text eol=lf

# Windows 배치 파일은 항상 CRLF
gradlew.bat text eol=crlf
*.bat text eol=crlf

# 바이너리 파일은 변환하지 않음
*.jar binary
*.webp binary
*.png binary
*.jpg binary
